"use client";

export { MusicProvider, useMusic } from "@/providers/MusicProvider";

// Re-export for backward compatibility
import { MusicProvider } from "@/providers/MusicProvider";

export default MusicProvider;
